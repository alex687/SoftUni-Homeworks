@model SportSystem.Web.ViewModels.HomeViewModel
@{
    ViewBag.Title = "Home Page";
}

<h2 class="text-info text-center">Come one big Scott. Easy easy weight!!</h2>

<div class="row">
    <div class="col-md-6">
        @Html.DisplayFor(m => m.Matches)
    </div>
    <div class="col-md-6">
        @Html.DisplayFor(m => m.Teams);
    </div>
</div>